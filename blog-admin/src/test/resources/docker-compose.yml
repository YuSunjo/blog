version: '3.1'

services:
  mysql:
    image: mysql:latest
    restart: "always"
    ports:
      - "13306:3306"
    environment:
      - MYSQL_ROOT_PASSWORD=test
      - MYSQL_DATABASE=test
      - MYSQL_USER=dev
      - MYSQL_PASSWORD=test
